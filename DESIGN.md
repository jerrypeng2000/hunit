# HUnit · 换算通（浏览器插件）产品与技术设计方案

> 版本：v2.0
> 日期：2026-08-19
> 状态：设计阶段
> 产品形态：Chrome / Edge 浏览器扩展（Manifest V3）
> 目标用户：跨境电商卖家、货代、采购、运营、独立站从业者

---

## 1. 产品定位（电商专家视角）

跨境电商卖家每天要处理的不只是“换算一个数”，而是三类完全不同的商品形态和对应的上架、物流格式：

- 只有长度的：数据线、HDMI 线、织带、LED 灯带、绳子、钢尺；
- 只有长和宽的：鼠标垫、海报、贴纸、地毯、屏幕膜、桌布；
- 长宽高都有的：包装箱、产品盒、家电、FBA 箱规。

HUnit 把这三类做成 **1D / 2D / 3D 三种模式**，用户选中模式后直接输入，插件自动完成单位换算、面积/体积/体积重计算，并输出可直接粘贴到 Amazon / eBay / 独立站的标准格式。

### 设计三原则

1. **按商品形态切换模式**，而不是让用户手动找单位；
2. **输出可直接粘贴的 Listing / 物流格式**；
3. **把“换算”升级为“上架尺寸 + 装箱体积重 + 定价”的一站式工作流**。

---

## 2. 目标用户与高频场景

| 用户 | 高频动作 |
|------|----------|
| 跨境电商卖家 | 填 Listing 尺寸、写箱规、算 FBA 体积重、成本定价 |
| 货代 / 物流 | 体积重、计费重、泡货判断 |
| 采购 / 跟单 | 多 SKU 尺寸批量换算 |
| 运营 / 独立站 | 汇率、多站点单位转换 |

---

## 3. 三种维度模式（核心）

### 3.1 1D 长度模式

适用于线材、管材、织带、灯带、绳、尺、皮带等只有长度的商品。

- 单位：mm / cm / m / in / ft / yd；
- 双向换算，结果格式：`3.3 ft / 1.0 m`、`100 cm / 39.37 in`；
- 常用快选：0.25m / 0.5m / 1m / 2m / 3m / 6ft / 10ft。

### 3.2 2D 长×宽模式

适用于鼠标垫、海报、贴纸、地毯、屏幕膜、桌布、布料、纸张等平面商品。

- 单位：cm×cm / in×in / mm×mm / ft×ft；
- 长和宽同时换算，结果格式：

```text
长  12.00 in / 30.48 cm
宽   8.00 in / 20.32 cm
面积  96.00 in² / 619.35 cm²
```

### 3.3 3D 长×宽×高模式

适用于包装箱、产品盒、家电、箱规。

- 长、宽、高同时换算，结果格式：

```text
长  12.00 in / 30.48 cm
宽   6.00 in / 15.24 cm
高   1.00 in /  2.54 cm
体积  72.00 in³ / 1179.87 cm³
体积重 0.24 kg / 0.52 lb（cm³ ÷ 5000）
```

- 体积重除数可切换：cm³ ÷ 5000 / 6000，in³ ÷ 139 / 166。

---

## 4. 两种使用场景

### 4.1 Listing 上架模式

- 按目标站点默认单位：美站用 in / lb / oz，欧日站用 cm / kg；
- 一键复制标准格式：
  - `12 x 6 x 1 inches`
  - `30.5 x 15.2 x 2.5 cm`
- 自动补全单位描述，降低填错概率。

### 4.2 物流 / 箱规模式

- 输入包装尺寸，输出体积、体积重、计费重；
- 支持不同物流商的计费重规则切换；
- 自动判断泡货 / 重货并提示。

---

## 5. 重量换算

- g ↔ kg ↔ lb ↔ oz 双向换算；
- 快捷值：50 / 100 / 200 / 500 / 1000 g，1 / 2 / 3 / 5 lb；
- 计费重 = max(实重, 体积重)。

---

## 6. 汇率与定价

### 6.1 汇率

- 实时汇率：USD / CNY / EUR / GBP / JPY / AUD / CAD；
- 金额换算：`金额 × 汇率 = 目标币种`；
- 手动汇率（付费）；
- 缓存 10 分钟，接口异常时使用上次缓存并显示更新时间。

### 6.2 成本定价器

输入采购成本、目标利润率、平台佣金，自动计算建议售价：

```text
成本：¥15.00
汇率：7.20
目标利润：30%
平台佣金：15%
建议售价：$4.08（¥29.37）
```

---

## 7. 智能识别与批量

- 粘贴自动解析：`12x6x1`、`30*15*2cm`、`3.3ft`、`12"`；
- 批量表格：粘贴 SKU + 尺寸多行，批量转换并导出；
- 历史记录 + 收藏，可一键重新载入。

---

## 8. 常用表维护

- 分类、标签，完整增删改查；
- 预设分类：线材、平板贴膜、包装箱、家居、3C、服装辅料；
- 每条记录支持 1D / 2D / 3D 类型，保存对应字段；
- 搜索、置顶、排序、一键载入换算区；
- CSV / JSON 导入导出（付费）；
- 云同步（付费）。

---

## 9. 效率与交互

- **实时换算**：输入即出结果，不需要点“换算”按钮；
- **逐行 / 整卡复制**；
- 全局快捷键（默认 Alt+U）；
- 右键菜单：选中数字直接换算；
- 页面划词识别：自动识别 `12cm`、`3.5in`、`500g`、`$20`；
- 记住上次模式和单位，减少重复设置。

---

## 10. 免费版 vs 付费版

| 功能 | 免费版 | 付费版（2 元/月） |
|------|--------|------------------|
| 每日换算次数 | 10 次 | 无限次 |
| 1D / 2D / 3D 基础换算 | ✅ | ✅ |
| 成本定价器 | ❌ | ✅ |
| 手动汇率 | ❌ | ✅ |
| 常用表数量 | 10 条 | 无限 |
| 批量换算 | ❌ | ✅ |
| CSV / JSON 导入导出 | ❌ | ✅ |
| 云同步 | ❌ | ✅ |
| 去除品牌水印 | ❌ | ✅ |

**配额说明**：每日 10 次和订阅状态由服务端校验，客户端只做缓存，避免清缓存绕过。

---

## 11. 技术架构

```text
┌────────────────────────────────────────────┐
│ 浏览器插件（Manifest V3）                    │
│ • Popup / Options：React + TypeScript + Vite │
│ • Service Worker：汇率抓取、缓存、配额校验    │
│ • Content Script：划词识别、悬浮换算          │
│ • 本地换算引擎：单位、面积、体积、体积重       │
│ • 智能解析：12x6x1 / 30*15*2cm / 3.3ft       │
└──────────────────┬─────────────────────────┘
                   │ HTTPS
        ┌──────────┴──────────┐
        │ 极薄服务端             │
        │ 账号、每日配额、订阅态  │
        └──────────┬──────────┘
                   │
        ┌──────────┴──────────┐
        │ ajk-api / 独立计费后端 │
        │ 2 元/月订阅、会员校验  │
        └─────────────────────┘
```

- 所有单位、面积、体积、体积重计算均在客户端完成；
- 汇率通过服务端代理或公开汇率接口获取并缓存；
- 每日配额和付费状态必须服务端校验。

---

## 12. 数据模型（chrome.storage JSON）

```json
{
  "settings": {
    "dimensionMode": "1d|2d|3d",
    "scenario": "listing|logistics",
    "defaultUnit": "cm|in",
    "defaultCurrency": "CNY|USD",
    "precision": 2,
    "divisor": 5000,
    "hotkey": "Alt+U"
  },
  "favorites": [
    {
      "id": "uuid",
      "name": "手机壳 12cm",
      "category": "3C",
      "tags": ["手机壳", "热销"],
      "mode": "3d",
      "length": 12,
      "width": 6,
      "height": 1,
      "unit": "cm",
      "weightGram": 80,
      "currency": "CNY",
      "amount": 19.9,
      "note": "",
      "pinned": false,
      "sortOrder": 0
    }
  ],
  "history": [
    {
      "id": "uuid",
      "type": "dimension|weight|currency|pricing",
      "input": {},
      "output": {},
      "timestamp": 0
    }
  ],
  "subscription": {
    "tier": "free|pro",
    "expiresAt": 0,
    "token": ""
  },
  "dailyUsage": {
    "date": "2026-08-19",
    "count": 0
  }
}
```

---

## 13. 汇率与数据策略

- 默认每 10 分钟刷新汇率，支持多币种批量拉取；
- 手动汇率兜底；
- 显示“数据更新时间”；
- 汇率请求走 HTTPS，支持可配置代理。

---

## 14. 隐私与安全

- 最小权限：`storage`、`alarms`、`contextMenus`，按需申请 `activeTab`；
- 不上传具体换算内容，服务端只处理订阅态和每日次数；
- 常用表默认存本地，付费云同步时加密传输。

---

## 15. 开发路线图

- **V1**：1D / 2D / 3D 换算 + 重量 + 汇率 + 常用表 CRUD + 免费 10 次/天 + 2 元包月；
- **V2**：划词换算、批量换算、手动汇率、成本定价器、导入导出；
- **V3**：云同步、更多币种、多设备/团队协作。

---

## 16. 验收标准与待确认事项

验收：

- [ ] 1D / 2D / 3D 三种模式可切换，长宽高可同时换算；
- [ ] 结果按 `xx cm / xx in` 展示，并自动计算面积/体积/体积重；
- [ ] 克 ↔ 磅换算正确，汇率实时可查；
- [ ] 常用表支持分类、标签、增删改查；
- [ ] 免费版每天 10 次，付费版无限次。

待确认：

- [ ] 汇率数据源（公开 API 还是自建代理）；
- [ ] 计费对接 ajk-api 还是独立轻后端；
- [ ] 首版是否就支持批量换算和划词换算；
- [ ] 上架 Chrome Web Store / Edge 商店的账号与软著安排。
